<!-- {{ page.header.title }} Section -->
<div class="content">
    <div class="carousel">
        {% for img in page.media.images %}
            <img src="{{ img.url }}" />
        {% endfor %}
    </div>
</div>
